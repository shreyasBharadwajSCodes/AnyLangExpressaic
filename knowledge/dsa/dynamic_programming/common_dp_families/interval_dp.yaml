id: interval_dp
title: Interval DP
category: dsa
level: advanced
languages:
  - python
  - cpp
  - java
tags:
  - interval_dp
  - dynamic_programming
  - interview
goal: Teach interval dp across Python, C++, and Java with state definition, recurrence writing, base cases, computation order, and interview explanation.
assets:
  interval_dp_trace:
    type: image
    path: assets/dsa/interval_dp/interval-dp-trace.png
    alt: Interval DP state table and transition trace.
lesson:
  - explain:
      title: Solve
      body: >
        Solve: Given items that must be ordered by start time, end time, value, frequency, or priority,
        sort them and make one pass with a chosen invariant.
  - compare_table:
      title: When Should This Come To Mind?
      columns:
        - When The Problem Looks Like This
        - Reach For
        - First Coding Move
      rows:
        -
          - "   gives intervals, ordering, custom ranking, minimum removals, overlap, schedule, or greedy choice."
          - " sorting, comparator, greedy, merge intervals, sweep line, or counting sort."
          - "Explain why the sort key makes future choices safe and how ties are handled."
        -
          - "A local choice becomes safe after sorting by the right key."
          - ""
          - ""
        -
          - "The hard part is choosing the key and tie-break."
          - ""
          - ""
  - explain:
      title: Core Mental Model
      body: >
        Interval DP solves every subarray or substring range. The current interval depends on smaller intervals inside it, usually through a split point.
  - explain:
      title: How The Algorithm Works
      body: >
        Fill by increasing interval length so smaller ranges are ready. For each left and right boundary, try every split and combine the two solved sides with the cost of the current interval.
  - compare_table:
      title: Variables And Transitions
      columns:
        - Variable Or Entity
        - Meaning
        - Interview Use
      rows:
        -
          - "length loop"
          - "build small to large"
          - "dependencies ready"
        -
          - "split"
          - "partition point"
          - "tries all structures"
        -
          - "current cost"
          - "range-level cost"
          - "added after children"
  - language_lens:
      language: python
      title: Python DP Strategy
      body: >
        Python makes memoization concise and tables easy, but row aliasing, recursion depth, and mutable cache keys can break correct ideas.
      default_instincts:
        - Use lru_cache or cache for top-down DP with hashable state.
        - Use row comprehensions for two-dimensional tables.
        - Use large numeric sentinels for minimum problems.
      behind_the_scenes:
        - Cached function arguments must be hashable.
        - Multiplying nested lists aliases rows.
        - Python recursion depth can fail on long state chains.
      avoid:
        - Passing mutable lists into cached functions.
        - Rebuilding slices inside transitions.
        - Forgetting whether dp means prefix answer or suffix answer.
  - language_lens:
      language: cpp
      title: C++ DP Strategy
      body: >
        C++ DP is fast when tables are passed by reference and numeric types are chosen deliberately.
      default_instincts:
        - Use vector tables and pass them by reference.
        - Use long long for counts, costs, and sums when constraints can grow.
        - Use safe INF values and guard before adding.
      behind_the_scenes:
        - Passing vectors by value copies the table.
        - Large local arrays can overflow the stack.
        - int overflow can silently change answers.
      avoid:
        - Updating a one-dimensional table in the wrong direction.
        - Using INT_MAX and then adding a cost.
        - Copying memo tables into recursive helpers.
  - language_lens:
      language: java
      title: Java DP Strategy
      body: >
        Java DP is clearest with primitive arrays, explicit initialization, and helper methods for memoized recursion.
      default_instincts:
        - Use int arrays or long arrays for bounded states.
        - Use Arrays.fill for sentinel initialization.
        - Use helper methods with shared memo fields or parameters.
      behind_the_scenes:
        - Two-dimensional arrays are arrays of rows.
        - Integer objects allow null sentinels but add boxing.
        - Recursive DP can overflow the Java call stack.
      avoid:
        - Filling only the outer array and forgetting each row.
        - Using int when the result needs long.
        - Reallocating tables inside recursive calls.
examples:
  - code_compare:
      title: Interval DP Core Code Shape
      idea: Track left, right, length, split point, and interval cost.
      python: |
        for length in range(2, n + 1):
            for left in range(0, n - length + 1):
                right = left + length - 1
                dp[left][right] = float("inf")
                for mid in range(left, right):
                    # Split interval into two solved smaller intervals.
                    cost = dp[left][mid] + dp[mid + 1][right] + merge_cost(left, right)
                    dp[left][right] = min(dp[left][right], cost)
      cpp: |
        for (int length = 2; length <= n; length++) {
            for (int left = 0; left + length - 1 < n; left++) {
                int right = left + length - 1;
                dp[left][right] = INF;
                for (int mid = left; mid < right; mid++) {
                    // Split interval into two solved smaller intervals.
                    int cost = dp[left][mid] + dp[mid + 1][right] + mergeCost(left, right);
                    dp[left][right] = min(dp[left][right], cost);
                }
            }
        }
      java: |
        for (int length = 2; length <= n; length++) {
            for (int left = 0; left + length - 1 < n; left++) {
                int right = left + length - 1;
                dp[left][right] = INF;
                for (int mid = left; mid < right; mid++) {
                    // Split interval into two solved smaller intervals.
                    int cost = dp[left][mid] + dp[mid + 1][right] + mergeCost(left, right);
                    dp[left][right] = Math.min(dp[left][right], cost);
                }
            }
        }
      note: >
        Read the code by first saying what each state means, then checking that every transition uses already valid smaller states.
transitions:
  python_to_cpp:
    title: Python Interval DP To C++
    mindset_shift:
      - Replace decorator caches with explicit memo tables or vectors.
      - Choose int, long long, and INF deliberately.
      - Pass tables and inputs by reference to avoid copying.
    habit_swaps:
      - python: Use lru_cache for recursive state.
        cpp: Use vector memo passed by reference.
    false_friends:
      - term: overflow
        warning: C++ int can overflow in counts, sums, and sentinel additions.
  python_to_java:
    title: Python Interval DP To Java
    mindset_shift:
      - Replace compact list setup with explicit arrays and Arrays.fill.
      - Use primitive arrays for speed when dimensions are known.
      - Keep memo state shared across helper calls.
    habit_swaps:
      - python: Use list comprehension for tables.
        java: Allocate arrays and fill each row when needed.
    false_friends:
      - term: boxed memo
        warning: Integer arrays allow null but cost more than primitive arrays with a sentinel.
interview_questions:
  - question: What clue points to Interval DP?
    answer: solving ranges by splitting them into smaller ranges.
  - question: What must you say before coding?
    answer: Define left, right, length, split point, and interval cost, then state the recurrence and base cases.
  - question: What is the common bug?
    answer: Using a transition before its dependency is ready, or changing one-dimensional update direction incorrectly.
revision:
  quick_summary:
    - "Interval DP is the DP tool for solving ranges by splitting them into smaller ranges."
    - "Working idea: Fill by increasing interval length so smaller ranges are ready. For each left and right boundary, try every split and combine the two solved sides with the cost of the current interval."
  cheat_table:
    title: Interval DP Revision Table
    columns:
      - What You Notice
      - State
      - Caveat
    rows:
      -
        - "solving ranges by splitting them into smaller ranges"
        - "left, right, length, split point, and interval cost"
        - "Define state in one sentence before writing code."
      -
        - "language risk"
        - "table and numeric type"
        - "Watch Python row aliasing, C++ copies and overflow, Java initialization."
  flashcards:
    - question: What is the most important DP interview sentence?
      answer: Say exactly what the state means.
notes:
  - "DP code should be read as state definition, base case, transition, fill order, answer location."
  - Use the clear table first; optimize space only after the dependency direction is obvious.
tlm_hooks:
  possible_tasks:
    - trace_interval_dp
    - generate_interval_dp_drills
  context: Focus on left, right, length, split point, and interval cost.
