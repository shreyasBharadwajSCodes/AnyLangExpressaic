id: tree_dp
title: Tree DP
category: dsa
level: advanced
languages:
  - python
  - cpp
  - java
tags:
  - tree_dp
  - dfs
  - dynamic_programming
  - interview
goal: Teach tree dp across Python, C++, and Java with state definition, recurrence writing, base cases, computation order, and interview explanation.
assets:
  tree_dp_trace:
    type: image
    path: assets/dsa/tree_dp/tree-dp-trace.png
    alt: Tree DP state table and transition trace.
lesson:
  - explain:
      title: Solve
      body: >
        Solve: Given a tree or prefix structure, solve by passing information down, collecting
        information up, or traversing in the required order.
  - compare_table:
      title: When Should This Come To Mind?
      columns:
        - When The Problem Looks Like This
        - Reach For
        - First Coding Move
      rows:
        -
          - "If the interviewer gives you root, child pointers, prefix words, ancestor relation, height, depth, or level order."
          - "A strong first thought is DFS, BFS, recursion with return value, BST bounds, trie path, or LCA."
          - "Say what each recursive call returns and what information is carried from ancestors."
        -
          - "A node answer depends on children, ancestors, or path state."
          - ""
          - ""
        -
          - "Null or terminal-node handling changes correctness."
          - ""
          - ""
  - explain:
      title: Core Mental Model
      body: >
        Tree DP is DFS where each node returns more than one state. The parent combines child states according to whether the current node is chosen or skipped.
  - explain:
      title: How The Algorithm Works
      body: >
        Define the meaning of each returned state. For each node, get child states, combine them, and return the node states upward. Keep global answers only when the answer is not just the root state.
  - compare_table:
      title: Variables And Transitions
      columns:
        - Variable Or Entity
        - Meaning
        - Interview Use
      rows:
        -
          - "take state"
          - "current node chosen"
          - "children may be restricted"
        -
          - "skip state"
          - "current node not chosen"
          - "children choose best"
        -
          - "child tuple"
          - "subtree summary"
          - "parent combines"
  - language_lens:
      language: python
      title: Python DP Strategy
      body: >
        Python makes memoization concise and tables easy, but row aliasing, recursion depth, and mutable cache keys can break correct ideas.
      default_instincts:
        - Use lru_cache or cache for top-down DP with hashable state.
        - Use row comprehensions for two-dimensional tables.
        - Use large numeric sentinels for minimum problems.
      behind_the_scenes:
        - Cached function arguments must be hashable.
        - Multiplying nested lists aliases rows.
        - Python recursion depth can fail on long state chains.
      avoid:
        - Passing mutable lists into cached functions.
        - Rebuilding slices inside transitions.
        - Forgetting whether dp means prefix answer or suffix answer.
  - language_lens:
      language: cpp
      title: C++ DP Strategy
      body: >
        C++ DP is fast when tables are passed by reference and numeric types are chosen deliberately.
      default_instincts:
        - Use vector tables and pass them by reference.
        - Use long long for counts, costs, and sums when constraints can grow.
        - Use safe INF values and guard before adding.
      behind_the_scenes:
        - Passing vectors by value copies the table.
        - Large local arrays can overflow the stack.
        - int overflow can silently change answers.
      avoid:
        - Updating a one-dimensional table in the wrong direction.
        - Using INT_MAX and then adding a cost.
        - Copying memo tables into recursive helpers.
  - language_lens:
      language: java
      title: Java DP Strategy
      body: >
        Java DP is clearest with primitive arrays, explicit initialization, and helper methods for memoized recursion.
      default_instincts:
        - Use int arrays or long arrays for bounded states.
        - Use Arrays.fill for sentinel initialization.
        - Use helper methods with shared memo fields or parameters.
      behind_the_scenes:
        - Two-dimensional arrays are arrays of rows.
        - Integer objects allow null sentinels but add boxing.
        - Recursive DP can overflow the Java call stack.
      avoid:
        - Filling only the outer array and forgetting each row.
        - Using int when the result needs long.
        - Reallocating tables inside recursive calls.
examples:
  - code_compare:
      title: Tree DP Core Code Shape
      idea: Track node, child states, take state, skip state, and returned tuple.
      python: |
        def dfs(node):
            if node is None:
                return (0, 0)

            left_take, left_skip = dfs(node.left)
            right_take, right_skip = dfs(node.right)
            # take forbids taking children; skip allows best child choice.
            take = node.val + left_skip + right_skip
            skip = max(left_take, left_skip) + max(right_take, right_skip)
            return (take, skip)
      cpp: |
        pair<int,int> dfs(TreeNode* node) {
            if (node == nullptr) return {0, 0};

            auto [leftTake, leftSkip] = dfs(node->left);
            auto [rightTake, rightSkip] = dfs(node->right);
            // take forbids taking children; skip allows best child choice.
            int take = node->val + leftSkip + rightSkip;
            int skip = max(leftTake, leftSkip) + max(rightTake, rightSkip);
            return {take, skip};
        }
      java: |
        private int[] dfs(TreeNode node) {
            if (node == null) return new int[]{0, 0};

            int[] left = dfs(node.left);
            int[] right = dfs(node.right);
            // take forbids taking children; skip allows best child choice.
            int take = node.val + left[1] + right[1];
            int skip = Math.max(left[0], left[1]) + Math.max(right[0], right[1]);
            return new int[]{take, skip};
        }
      note: >
        Read the code by first saying what each state means, then checking that every transition uses already valid smaller states.
transitions:
  python_to_cpp:
    title: Python Tree DP To C++
    mindset_shift:
      - Replace decorator caches with explicit memo tables or vectors.
      - Choose int, long long, and INF deliberately.
      - Pass tables and inputs by reference to avoid copying.
    habit_swaps:
      - python: Use lru_cache for recursive state.
        cpp: Use vector memo passed by reference.
    false_friends:
      - term: overflow
        warning: C++ int can overflow in counts, sums, and sentinel additions.
  python_to_java:
    title: Python Tree DP To Java
    mindset_shift:
      - Replace compact list setup with explicit arrays and Arrays.fill.
      - Use primitive arrays for speed when dimensions are known.
      - Keep memo state shared across helper calls.
    habit_swaps:
      - python: Use list comprehension for tables.
        java: Allocate arrays and fill each row when needed.
    false_friends:
      - term: boxed memo
        warning: Integer arrays allow null but cost more than primitive arrays with a sentinel.
interview_questions:
  - question: What clue points to Tree DP?
    answer: computing multiple states per subtree.
  - question: What must you say before coding?
    answer: Define node, child states, take state, skip state, and returned tuple, then state the recurrence and base cases.
  - question: What is the common bug?
    answer: Using a transition before its dependency is ready, or changing one-dimensional update direction incorrectly.
revision:
  quick_summary:
    - "Tree DP is the DP tool for computing multiple states per subtree."
    - "Working idea: Define the meaning of each returned state. For each node, get child states, combine them, and return the node states upward. Keep global answers only when the answer is not just the root state."
  cheat_table:
    title: Tree DP Revision Table
    columns:
      - What You Notice
      - State
      - Caveat
    rows:
      -
        - "computing multiple states per subtree"
        - "node, child states, take state, skip state, and returned tuple"
        - "Define state in one sentence before writing code."
      -
        - "language risk"
        - "table and numeric type"
        - "Watch Python row aliasing, C++ copies and overflow, Java initialization."
  flashcards:
    - question: What is the most important DP interview sentence?
      answer: Say exactly what the state means.
notes:
  - "DP code should be read as state definition, base case, transition, fill order, answer location."
  - Use the clear table first; optimize space only after the dependency direction is obvious.
tlm_hooks:
  possible_tasks:
    - trace_tree_dp
    - generate_tree_dp_drills
  context: Focus on node, child states, take state, skip state, and returned tuple.
