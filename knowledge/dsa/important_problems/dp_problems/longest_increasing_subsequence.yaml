id: longest_increasing_subsequence
title: Longest Increasing Subsequence
category: dsa
level: important_problem
languages:
  - python
  - cpp
  - java
tags:
  - important_problem
  - interview
  - dp_problems
goal: Teach Longest Increasing Subsequence as an interview problem with recognition cue, approach, state tracking, edge cases, coordinated code, and language caveats.
assets:
  longest_increasing_subsequence_trace:
    type: image
    path: assets/dsa/longest_increasing_subsequence/longest-increasing-subsequence-trace.png
    alt: Longest Increasing Subsequence variable trace and interview dry run.
lesson:
  - explain:
      title: Solve
      body: >
        Solve: Given an integer array, return the length of the longest strictly increasing subsequence.
  - compare_table:
      title: When Should This Come To Mind?
      columns:
        - When The Problem Looks Like This
        - Reach For
        - First Coding Move
      rows:
        -
          - "If your interviewer asks for count ways, min or max cost, choose or skip, subsequence, partition, or optimal value."
          - "A strong first thought is memoization, tabulation, one-dimensional DP, two-dimensional DP, knapsack, LIS, LCS, or interval DP."
          - "Write the state meaning in words before code, then verify the transition reads only already-known states."
        -
          - "The same suffix, prefix, index, capacity, or pair of indices repeats."
          - ""
          - ""
        -
          - "Greedy choice is not obviously safe."
          - ""
          - ""
  - explain:
      title: When To Use This
      body: >
        Bring this idea in when the problem is quietly asking for tails array with binary search. Before
        coding, say what the input shape is, what answer must be returned, and which
        state proves progress.
  - explain:
      title: Approach And State
      body: >
        Track tails array and insertion position. The important interview move is to explain when this state
        changes, why that timing is safe, and which cases are intentionally skipped
        or resolved.
  - compare_table:
      title: Variables And Edge Cases
      columns:
        - Variable Or Entity
        - Meaning
        - Interview Caveat
      rows:
        -
          - "state"
          - "tails array and insertion position"
          - "Name this before writing code."
        -
          - "update timing"
          - "the line where progress becomes official"
          - "Most bugs happen before or after this line."
        -
          - "edge case"
          - "empty, one item, duplicates, boundary, or unreachable state"
          - "Test it after the main dry run."
  - language_lens:
      language: python
      title: Python Strategy
      body: >
        Keep the solution compact, but make the state variable names explicit enough for a dry run.
      default_instincts:
        - Use dictionaries, sets, deque, heapq, or lists based on the pattern.
        - Prefer tuple state for immutable compound keys.
        - Watch list aliasing and recursion depth.
      avoid:
        - Hiding an O n operation inside a loop.
        - Using mutable objects as dictionary keys.
        - Updating the answer before the state is valid.
  - language_lens:
      language: cpp
      title: C++ Strategy
      body: >
        Choose containers and numeric types deliberately, and avoid copying large state inside helpers.
      default_instincts:
        - Pass large vectors, strings, and adjacency lists by reference.
        - Use long long when counts, products, sums, or distances can grow.
        - Configure comparators and heaps explicitly.
      avoid:
        - Passing vectors by value accidentally.
        - Using unsafe int sentinels with addition.
        - Dereferencing null pointers in linked-list or tree problems.
  - language_lens:
      language: java
      title: Java Strategy
      body: >
        Use explicit arrays, initialized collections, helper methods, and safe comparator or equality choices.
      default_instincts:
        - Use primitive arrays for fixed-size state.
        - Use ArrayDeque for queues and stacks.
        - Initialize every nested collection before adding to it.
      avoid:
        - Comparator subtraction when values can be large.
        - Double equals for string value comparison.
        - Forgetting null checks on node references.
examples:
  - code_compare:
      title: Longest Increasing Subsequence Core Implementation
      idea: tails array with binary search.
      python: |
        tails = []
        for x in nums:
            i = bisect_left(tails, x)
            if i == len(tails):
                tails.append(x)
            else:
                # Smaller tail for same length keeps future extension easier.
                tails[i] = x
      cpp: |
        vector<int> tails;
        for (int x : nums) {
            auto it = lower_bound(tails.begin(), tails.end(), x);
            if (it == tails.end()) tails.push_back(x);
            else {
                // Smaller tail for same length keeps future extension easier.
                *it = x;
            }
        }
      java: |
        int[] tails = new int[nums.length];
        int size = 0;
        for (int x : nums) {
            int i = Arrays.binarySearch(tails, 0, size, x);
            if (i < 0) i = -i - 1;
            // Smaller tail for same length keeps future extension easier.
            tails[i] = x;
            if (i == size) size++;
        }
      note: >
        Read the code by following tails array and insertion position. The comments mark non-trivial state
        changes only, so the explanation and implementation stay coordinated.
interview_questions:
  - question: What is the pattern clue for Longest Increasing Subsequence?
    answer: tails array with binary search.
  - question: What variables should you dry-run?
    answer: Dry-run tails array and insertion position, plus the answer variable and any boundary pointer.
  - question: What edge cases should you test?
    answer: Test empty or minimal input, duplicate values where relevant, boundary indices, and cases where no valid answer exists.
revision:
  quick_summary:
    - "Longest Increasing Subsequence is solved by tails array with binary search."
    - "Working idea: track tails array and insertion position, update it at the safe moment, and dry-run the edge case that can break the invariant."
  cheat_table:
    title: Longest Increasing Subsequence Quick Recall
    columns:
      - Remember
      - Why
    rows:
      -
        - "Name the state"
        - "The code becomes readable immediately."
      -
        - "Explain update timing"
        - "This proves correctness."
      -
        - "Test the smallest input"
        - "This catches most boundary mistakes."
  flashcards:
    - question: What should you say if asked why this works?
      answer: State the invariant and explain why each update preserves it.
notes:
  - Keep comments around meaningful state changes only.
  - In interviews, explain the brute force briefly, then point to the repeated work removed by this pattern.
tlm_hooks:
  possible_tasks:
    - trace_longest_increasing_subsequence
    - generate_longest_increasing_subsequence_drills
  context: Focus on tails array and insertion position.
