id: subarray_sum_equals_k
title: Subarray Sum Equals K
category: dsa
level: important_problem
languages:
  - python
  - cpp
  - java
tags:
  - important_problem
  - interview
  - hashing_and_grouping
goal: Teach Subarray Sum Equals K as an interview problem with recognition cue, approach, state tracking, edge cases, coordinated code, and language caveats.
assets:
  subarray_sum_equals_k_trace:
    type: image
    path: assets/dsa/subarray_sum_equals_k/subarray-sum-equals-k-trace.png
    alt: Subarray Sum Equals K variable trace and interview dry run.
lesson:
  - explain:
      title: Solve
      body: >
        Solve: Given an integer array nums and an integer k, return the number of continuous subarrays
        whose sum equals k.
  - compare_table:
      title: When Should This Come To Mind?
      columns:
        - When The Problem Looks Like This
        - Reach For
        - First Coding Move
      rows:
        -
          - "If the problem mentions subarray sums, range updates, product except self, maximum contiguous sum, or repeated queries."
          - " prefix sum, prefix count map, difference array, suffix pass, or Kadane state."
          - "Define what the running value means before and after processing the current index."
        -
          - "The desired answer can be expressed as current cumulative state minus earlier state."
          - ""
          - ""
        -
          - "Nested loops are only recomputing totals."
          - ""
          - ""
  - explain:
      title: When To Use This
      body: >
        Bring this idea in when the problem is quietly asking for prefix sum frequency counting. Before
        coding, say what the input shape is, what answer must be returned, and which
        state proves progress.
  - explain:
      title: Approach And State
      body: >
        Track running prefix and earlier prefix counts. The important interview move is to explain when this state
        changes, why that timing is safe, and which cases are intentionally skipped
        or resolved.
  - compare_table:
      title: Variables And Edge Cases
      columns:
        - Variable Or Entity
        - Meaning
        - Interview Caveat
      rows:
        -
          - "state"
          - "running prefix and earlier prefix counts"
          - "Name this before writing code."
        -
          - "update timing"
          - "the line where progress becomes official"
          - "Most bugs happen before or after this line."
        -
          - "edge case"
          - "empty, one item, duplicates, boundary, or unreachable state"
          - "Test it after the main dry run."
  - language_lens:
      language: python
      title: Python Strategy
      body: >
        Keep the solution compact, but make the state variable names explicit enough for a dry run.
      default_instincts:
        - Use dictionaries, sets, deque, heapq, or lists based on the pattern.
        - Prefer tuple state for immutable compound keys.
        - Watch list aliasing and recursion depth.
      avoid:
        - Hiding an O n operation inside a loop.
        - Using mutable objects as dictionary keys.
        - Updating the answer before the state is valid.
  - language_lens:
      language: cpp
      title: C++ Strategy
      body: >
        Choose containers and numeric types deliberately, and avoid copying large state inside helpers.
      default_instincts:
        - Pass large vectors, strings, and adjacency lists by reference.
        - Use long long when counts, products, sums, or distances can grow.
        - Configure comparators and heaps explicitly.
      avoid:
        - Passing vectors by value accidentally.
        - Using unsafe int sentinels with addition.
        - Dereferencing null pointers in linked-list or tree problems.
  - language_lens:
      language: java
      title: Java Strategy
      body: >
        Use explicit arrays, initialized collections, helper methods, and safe comparator or equality choices.
      default_instincts:
        - Use primitive arrays for fixed-size state.
        - Use ArrayDeque for queues and stacks.
        - Initialize every nested collection before adding to it.
      avoid:
        - Comparator subtraction when values can be large.
        - Double equals for string value comparison.
        - Forgetting null checks on node references.
examples:
  - code_compare:
      title: Subarray Sum Equals K Core Implementation
      idea: prefix sum frequency counting.
      python: |
        count = 0
        pref = 0
        freq = {0: 1}
        for x in nums:
            pref += x
            # Earlier prefix pref - k forms a subarray ending here with sum k.
            count += freq.get(pref - k, 0)
            freq[pref] = freq.get(pref, 0) + 1
      cpp: |
        long long count = 0, pref = 0;
        unordered_map<long long,int> freq;
        freq[0] = 1;
        for (int x : nums) {
            pref += x;
            // Earlier prefix pref - k forms a subarray ending here with sum k.
            count += freq[pref - k];
            freq[pref]++;
        }
      java: |
        long count = 0, pref = 0;
        Map<Long, Integer> freq = new HashMap<>();
        freq.put(0L, 1);
        for (int x : nums) {
            pref += x;
            // Earlier prefix pref - k forms a subarray ending here with sum k.
            count += freq.getOrDefault(pref - k, 0);
            freq.put(pref, freq.getOrDefault(pref, 0) + 1);
        }
      note: >
        Read the code by following running prefix and earlier prefix counts. The comments mark non-trivial state
        changes only, so the explanation and implementation stay coordinated.
interview_questions:
  - question: What is the pattern clue for Subarray Sum Equals K?
    answer: prefix sum frequency counting.
  - question: What variables should you dry-run?
    answer: Dry-run running prefix and earlier prefix counts, plus the answer variable and any boundary pointer.
  - question: What edge cases should you test?
    answer: Test empty or minimal input, duplicate values where relevant, boundary indices, and cases where no valid answer exists.
revision:
  quick_summary:
    - "Subarray Sum Equals K is solved by prefix sum frequency counting."
    - "Working idea: track running prefix and earlier prefix counts, update it at the safe moment, and dry-run the edge case that can break the invariant."
  cheat_table:
    title: Subarray Sum Equals K Quick Recall
    columns:
      - Remember
      - Why
    rows:
      -
        - "Name the state"
        - "The code becomes readable immediately."
      -
        - "Explain update timing"
        - "This proves correctness."
      -
        - "Test the smallest input"
        - "This catches most boundary mistakes."
  flashcards:
    - question: What should you say if asked why this works?
      answer: State the invariant and explain why each update preserves it.
notes:
  - Keep comments around meaningful state changes only.
  - In interviews, explain the brute force briefly, then point to the repeated work removed by this pattern.
tlm_hooks:
  possible_tasks:
    - trace_subarray_sum_equals_k
    - generate_subarray_sum_equals_k_drills
  context: Focus on running prefix and earlier prefix counts.
