id: longest_consecutive_sequence
title: Longest Consecutive Sequence
category: dsa
level: important_problem
languages:
  - python
  - cpp
  - java
tags:
  - important_problem
  - interview
  - hashing_and_grouping
goal: Teach Longest Consecutive Sequence as an interview problem with recognition cue, approach, state tracking, edge cases, coordinated code, and language caveats.
assets:
  longest_consecutive_sequence_trace:
    type: image
    path: assets/dsa/longest_consecutive_sequence/longest-consecutive-sequence-trace.png
    alt: Longest Consecutive Sequence variable trace and interview dry run.
lesson:
  - explain:
      title: Solve
      body: >
        Solve: Given an unsorted integer array, return the length of the longest consecutive elements
        sequence.
  - compare_table:
      title: When Should This Come To Mind?
      columns:
        - When The Problem Looks Like This
        - Reach For
        - First Coding Move
      rows:
        -
          - "   asks about existence, duplicates, grouping, frequency, complements, or fastest lookup."
          - " hash map, hash set, frequency map, compound key, or heap over counts."
          - "Choose the key carefully and say whether the state stores count, first index, last index, or existence."
        -
          - "Order is not the main requirement, but identity or counts are."
          - ""
          - ""
        -
          - "A scan with a map or set removes an inner loop."
          - ""
          - ""
  - explain:
      title: When To Use This
      body: >
        Bring this idea in when the problem is quietly asking for set runs that start only when predecessor is absent. Before
        coding, say what the input shape is, what answer must be returned, and which
        state proves progress.
  - explain:
      title: Approach And State
      body: >
        Track set membership and run start. The important interview move is to explain when this state
        changes, why that timing is safe, and which cases are intentionally skipped
        or resolved.
  - compare_table:
      title: Variables And Edge Cases
      columns:
        - Variable Or Entity
        - Meaning
        - Interview Caveat
      rows:
        -
          - "state"
          - "set membership and run start"
          - "Name this before writing code."
        -
          - "update timing"
          - "the line where progress becomes official"
          - "Most bugs happen before or after this line."
        -
          - "edge case"
          - "empty, one item, duplicates, boundary, or unreachable state"
          - "Test it after the main dry run."
  - language_lens:
      language: python
      title: Python Strategy
      body: >
        Keep the solution compact, but make the state variable names explicit enough for a dry run.
      default_instincts:
        - Use dictionaries, sets, deque, heapq, or lists based on the pattern.
        - Prefer tuple state for immutable compound keys.
        - Watch list aliasing and recursion depth.
      avoid:
        - Hiding an O n operation inside a loop.
        - Using mutable objects as dictionary keys.
        - Updating the answer before the state is valid.
  - language_lens:
      language: cpp
      title: C++ Strategy
      body: >
        Choose containers and numeric types deliberately, and avoid copying large state inside helpers.
      default_instincts:
        - Pass large vectors, strings, and adjacency lists by reference.
        - Use long long when counts, products, sums, or distances can grow.
        - Configure comparators and heaps explicitly.
      avoid:
        - Passing vectors by value accidentally.
        - Using unsafe int sentinels with addition.
        - Dereferencing null pointers in linked-list or tree problems.
  - language_lens:
      language: java
      title: Java Strategy
      body: >
        Use explicit arrays, initialized collections, helper methods, and safe comparator or equality choices.
      default_instincts:
        - Use primitive arrays for fixed-size state.
        - Use ArrayDeque for queues and stacks.
        - Initialize every nested collection before adding to it.
      avoid:
        - Comparator subtraction when values can be large.
        - Double equals for string value comparison.
        - Forgetting null checks on node references.
examples:
  - code_compare:
      title: Longest Consecutive Sequence Core Implementation
      idea: set runs that start only when predecessor is absent.
      python: |
        seen = set(nums)
        best = 0
        for x in seen:
            if x - 1 in seen:
                continue
            y = x
            while y in seen:
                y += 1
            # x is the start of this run because x minus one was absent.
            best = max(best, y - x)
      cpp: |
        unordered_set<int> seen(nums.begin(), nums.end());
        int best = 0;
        for (int x : seen) {
            if (seen.count(x - 1)) continue;
            int y = x;
            while (seen.count(y)) y++;
            // x is the start of this run because x - 1 was absent.
            best = max(best, y - x);
        }
      java: |
        Set<Integer> seen = new HashSet<>();
        for (int x : nums) seen.add(x);
        int best = 0;
        for (int x : seen) {
            if (seen.contains(x - 1)) continue;
            int y = x;
            while (seen.contains(y)) y++;
            // x is the start of this run because x - 1 was absent.
            best = Math.max(best, y - x);
        }
      note: >
        Read the code by following set membership and run start. The comments mark non-trivial state
        changes only, so the explanation and implementation stay coordinated.
interview_questions:
  - question: What is the pattern clue for Longest Consecutive Sequence?
    answer: set runs that start only when predecessor is absent.
  - question: What variables should you dry-run?
    answer: Dry-run set membership and run start, plus the answer variable and any boundary pointer.
  - question: What edge cases should you test?
    answer: Test empty or minimal input, duplicate values where relevant, boundary indices, and cases where no valid answer exists.
revision:
  quick_summary:
    - "Longest Consecutive Sequence is solved by set runs that start only when predecessor is absent."
    - "Working idea: track set membership and run start, update it at the safe moment, and dry-run the edge case that can break the invariant."
  cheat_table:
    title: Longest Consecutive Sequence Quick Recall
    columns:
      - Remember
      - Why
    rows:
      -
        - "Name the state"
        - "The code becomes readable immediately."
      -
        - "Explain update timing"
        - "This proves correctness."
      -
        - "Test the smallest input"
        - "This catches most boundary mistakes."
  flashcards:
    - question: What should you say if asked why this works?
      answer: State the invariant and explain why each update preserves it.
notes:
  - Keep comments around meaningful state changes only.
  - In interviews, explain the brute force briefly, then point to the repeated work removed by this pattern.
tlm_hooks:
  possible_tasks:
    - trace_longest_consecutive_sequence
    - generate_longest_consecutive_sequence_drills
  context: Focus on set membership and run start.
