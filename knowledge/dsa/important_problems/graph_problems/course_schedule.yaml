id: course_schedule
title: Course Schedule
category: dsa
level: important_problem
languages:
  - python
  - cpp
  - java
tags:
  - important_problem
  - interview
  - graph_problems
goal: Teach Course Schedule as an interview problem with recognition cue, approach, state tracking, edge cases, coordinated code, and language caveats.
assets:
  course_schedule_trace:
    type: image
    path: assets/dsa/course_schedule/course-schedule-trace.png
    alt: Course Schedule variable trace and interview dry run.
lesson:
  - explain:
      title: Solve
      body: >
        Solve: Given course prerequisites, determine whether it is possible to finish all courses.
  - compare_table:
      title: When Should This Come To Mind?
      columns:
        - When The Problem Looks Like This
        - Reach For
        - First Coding Move
      rows:
        -
          - "   mentions reachability, connected groups, prerequisites, shortest path, cycle, grid neighbors, or components."
          - " BFS, DFS, topological sort, union find, Dijkstra, Bellman-Ford, Floyd-Warshall, or MST."
          - "Define the graph representation, when a node becomes visited, and what each edge update means."
        -
          - "The same state can be reached from multiple directions."
          - ""
          - ""
        -
          - "Visited timing or distance relaxation is the correctness risk."
          - ""
          - ""
  - explain:
      title: When To Use This
      body: >
        Bring this idea in when the problem is quietly asking for topological sort over prerequisite graph. Before
        coding, say what the input shape is, what answer must be returned, and which
        state proves progress.
  - explain:
      title: Approach And State
      body: >
        Track indegree, adjacency list, processed count. The important interview move is to explain when this state
        changes, why that timing is safe, and which cases are intentionally skipped
        or resolved.
  - compare_table:
      title: Variables And Edge Cases
      columns:
        - Variable Or Entity
        - Meaning
        - Interview Caveat
      rows:
        -
          - "state"
          - "indegree, adjacency list, processed count"
          - "Name this before writing code."
        -
          - "update timing"
          - "the line where progress becomes official"
          - "Most bugs happen before or after this line."
        -
          - "edge case"
          - "empty, one item, duplicates, boundary, or unreachable state"
          - "Test it after the main dry run."
  - language_lens:
      language: python
      title: Python Strategy
      body: >
        Keep the solution compact, but make the state variable names explicit enough for a dry run.
      default_instincts:
        - Use dictionaries, sets, deque, heapq, or lists based on the pattern.
        - Prefer tuple state for immutable compound keys.
        - Watch list aliasing and recursion depth.
      avoid:
        - Hiding an O n operation inside a loop.
        - Using mutable objects as dictionary keys.
        - Updating the answer before the state is valid.
  - language_lens:
      language: cpp
      title: C++ Strategy
      body: >
        Choose containers and numeric types deliberately, and avoid copying large state inside helpers.
      default_instincts:
        - Pass large vectors, strings, and adjacency lists by reference.
        - Use long long when counts, products, sums, or distances can grow.
        - Configure comparators and heaps explicitly.
      avoid:
        - Passing vectors by value accidentally.
        - Using unsafe int sentinels with addition.
        - Dereferencing null pointers in linked-list or tree problems.
  - language_lens:
      language: java
      title: Java Strategy
      body: >
        Use explicit arrays, initialized collections, helper methods, and safe comparator or equality choices.
      default_instincts:
        - Use primitive arrays for fixed-size state.
        - Use ArrayDeque for queues and stacks.
        - Initialize every nested collection before adding to it.
      avoid:
        - Comparator subtraction when values can be large.
        - Double equals for string value comparison.
        - Forgetting null checks on node references.
examples:
  - code_compare:
      title: Course Schedule Core Implementation
      idea: topological sort over prerequisite graph.
      python: |
        q = deque([i for i in range(n) if indeg[i] == 0])
        done = 0
        while q:
            pre = q.popleft()
            done += 1
            for course in adj[pre]:
                indeg[course] -= 1
                if indeg[course] == 0:
                    # All prerequisites for this course have been removed.
                    q.append(course)
        return done == n
      cpp: |
        queue<int> q;
        for (int i = 0; i < n; i++) if (indeg[i] == 0) q.push(i);
        int done = 0;
        while (!q.empty()) {
            int pre = q.front();
            q.pop();
            done++;
            for (int course : adj[pre]) {
                if (--indeg[course] == 0) {
                    // All prerequisites for this course have been removed.
                    q.push(course);
                }
            }
        }
        return done == n;
      java: |
        Deque<Integer> q = new ArrayDeque<>();
        for (int i = 0; i < n; i++) if (indeg[i] == 0) q.offer(i);
        int done = 0;
        while (!q.isEmpty()) {
            int pre = q.poll();
            done++;
            for (int course : adj.get(pre)) {
                if (--indeg[course] == 0) {
                    // All prerequisites for this course have been removed.
                    q.offer(course);
                }
            }
        }
        return done == n;
      note: >
        Read the code by following indegree, adjacency list, processed count. The comments mark non-trivial state
        changes only, so the explanation and implementation stay coordinated.
interview_questions:
  - question: What is the pattern clue for Course Schedule?
    answer: topological sort over prerequisite graph.
  - question: What variables should you dry-run?
    answer: Dry-run indegree, adjacency list, processed count, plus the answer variable and any boundary pointer.
  - question: What edge cases should you test?
    answer: Test empty or minimal input, duplicate values where relevant, boundary indices, and cases where no valid answer exists.
revision:
  quick_summary:
    - "Course Schedule is solved by topological sort over prerequisite graph."
    - "Working idea: track indegree, adjacency list, processed count, update it at the safe moment, and dry-run the edge case that can break the invariant."
  cheat_table:
    title: Course Schedule Quick Recall
    columns:
      - Remember
      - Why
    rows:
      -
        - "Name the state"
        - "The code becomes readable immediately."
      -
        - "Explain update timing"
        - "This proves correctness."
      -
        - "Test the smallest input"
        - "This catches most boundary mistakes."
  flashcards:
    - question: What should you say if asked why this works?
      answer: State the invariant and explain why each update preserves it.
notes:
  - Keep comments around meaningful state changes only.
  - In interviews, explain the brute force briefly, then point to the repeated work removed by this pattern.
tlm_hooks:
  possible_tasks:
    - trace_course_schedule
    - generate_course_schedule_drills
  context: Focus on indegree, adjacency list, processed count.
