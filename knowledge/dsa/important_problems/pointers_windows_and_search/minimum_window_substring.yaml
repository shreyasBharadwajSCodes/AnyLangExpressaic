id: minimum_window_substring
title: Minimum Window Substring
category: dsa
level: important_problem
languages:
  - python
  - cpp
  - java
tags:
  - important_problem
  - interview
  - pointers_windows_and_search
goal: Teach Minimum Window Substring as an interview problem with recognition cue, approach, state tracking, edge cases, coordinated code, and language caveats.
assets:
  minimum_window_substring_trace:
    type: image
    path: assets/dsa/minimum_window_substring/minimum-window-substring-trace.png
    alt: Minimum Window Substring variable trace and interview dry run.
lesson:
  - explain:
      title: Solve
      body: >
        Solve: Given strings s and t, return the minimum window in s that contains every character of t
        with required frequency.
  - compare_table:
      title: When Should This Come To Mind?
      columns:
        - When The Problem Looks Like This
        - Reach For
        - First Coding Move
      rows:
        -
          - "   says contiguous subarray, substring, at most, at least, exactly, longest, shortest, or window."
          - " fixed window, variable window, frequency map, or monotonic deque."
          - "Track what the window currently contains and shrink only when the invariant tells you to."
        -
          - "The answer changes when one boundary enters or leaves."
          - ""
          - ""
        -
          - "Recomputing each segment would repeat the same work."
          - ""
          - ""
  - explain:
      title: When To Use This
      body: >
        Bring this idea in when the problem is quietly asking for variable window satisfying required counts. Before
        coding, say what the input shape is, what answer must be returned, and which
        state proves progress.
  - explain:
      title: Approach And State
      body: >
        Track need counts, missing count, left and right. The important interview move is to explain when this state
        changes, why that timing is safe, and which cases are intentionally skipped
        or resolved.
  - compare_table:
      title: Variables And Edge Cases
      columns:
        - Variable Or Entity
        - Meaning
        - Interview Caveat
      rows:
        -
          - "state"
          - "need counts, missing count, left and right"
          - "Name this before writing code."
        -
          - "update timing"
          - "the line where progress becomes official"
          - "Most bugs happen before or after this line."
        -
          - "edge case"
          - "empty, one item, duplicates, boundary, or unreachable state"
          - "Test it after the main dry run."
  - language_lens:
      language: python
      title: Python Strategy
      body: >
        Keep the solution compact, but make the state variable names explicit enough for a dry run.
      default_instincts:
        - Use dictionaries, sets, deque, heapq, or lists based on the pattern.
        - Prefer tuple state for immutable compound keys.
        - Watch list aliasing and recursion depth.
      avoid:
        - Hiding an O n operation inside a loop.
        - Using mutable objects as dictionary keys.
        - Updating the answer before the state is valid.
  - language_lens:
      language: cpp
      title: C++ Strategy
      body: >
        Choose containers and numeric types deliberately, and avoid copying large state inside helpers.
      default_instincts:
        - Pass large vectors, strings, and adjacency lists by reference.
        - Use long long when counts, products, sums, or distances can grow.
        - Configure comparators and heaps explicitly.
      avoid:
        - Passing vectors by value accidentally.
        - Using unsafe int sentinels with addition.
        - Dereferencing null pointers in linked-list or tree problems.
  - language_lens:
      language: java
      title: Java Strategy
      body: >
        Use explicit arrays, initialized collections, helper methods, and safe comparator or equality choices.
      default_instincts:
        - Use primitive arrays for fixed-size state.
        - Use ArrayDeque for queues and stacks.
        - Initialize every nested collection before adding to it.
      avoid:
        - Comparator subtraction when values can be large.
        - Double equals for string value comparison.
        - Forgetting null checks on node references.
examples:
  - code_compare:
      title: Minimum Window Substring Core Implementation
      idea: variable window satisfying required counts.
      python: |
        need = Counter(t)
        missing = len(t)
        left = 0
        best = None
        for right, ch in enumerate(s):
            if need[ch] > 0:
                missing -= 1
            need[ch] -= 1
            while missing == 0:
                if best is None or right - left + 1 < best[1] - best[0]:
                    # Window is valid here, so it is safe to update best.
                    best = (left, right + 1)
                need[s[left]] += 1
                if need[s[left]] > 0:
                    missing += 1
                left += 1
      cpp: |
        vector<int> need(128, 0);
        for (char ch : t) need[ch]++;
        int missing = t.size(), left = 0, bestL = 0, bestLen = INT_MAX;
        for (int right = 0; right < s.size(); right++) {
            if (need[s[right]] > 0) missing--;
            need[s[right]]--;
            while (missing == 0) {
                if (right - left + 1 < bestLen) {
                    // Window is valid here, so it is safe to update best.
                    bestL = left;
                    bestLen = right - left + 1;
                }
                need[s[left]]++;
                if (need[s[left]] > 0) missing++;
                left++;
            }
        }
      java: |
        int[] need = new int[128];
        for (char ch : t.toCharArray()) need[ch]++;
        int missing = t.length(), left = 0, bestL = 0, bestLen = Integer.MAX_VALUE;
        for (int right = 0; right < s.length(); right++) {
            if (need[s.charAt(right)] > 0) missing--;
            need[s.charAt(right)]--;
            while (missing == 0) {
                if (right - left + 1 < bestLen) {
                    // Window is valid here, so it is safe to update best.
                    bestL = left;
                    bestLen = right - left + 1;
                }
                need[s.charAt(left)]++;
                if (need[s.charAt(left)] > 0) missing++;
                left++;
            }
        }
      note: >
        Read the code by following need counts, missing count, left and right. The comments mark non-trivial state
        changes only, so the explanation and implementation stay coordinated.
interview_questions:
  - question: What is the pattern clue for Minimum Window Substring?
    answer: variable window satisfying required counts.
  - question: What variables should you dry-run?
    answer: Dry-run need counts, missing count, left and right, plus the answer variable and any boundary pointer.
  - question: What edge cases should you test?
    answer: Test empty or minimal input, duplicate values where relevant, boundary indices, and cases where no valid answer exists.
revision:
  quick_summary:
    - "Minimum Window Substring is solved by variable window satisfying required counts."
    - "Working idea: track need counts, missing count, left and right, update it at the safe moment, and dry-run the edge case that can break the invariant."
  cheat_table:
    title: Minimum Window Substring Quick Recall
    columns:
      - Remember
      - Why
    rows:
      -
        - "Name the state"
        - "The code becomes readable immediately."
      -
        - "Explain update timing"
        - "This proves correctness."
      -
        - "Test the smallest input"
        - "This catches most boundary mistakes."
  flashcards:
    - question: What should you say if asked why this works?
      answer: State the invariant and explain why each update preserves it.
notes:
  - Keep comments around meaningful state changes only.
  - In interviews, explain the brute force briefly, then point to the repeated work removed by this pattern.
tlm_hooks:
  possible_tasks:
    - trace_minimum_window_substring
    - generate_minimum_window_substring_drills
  context: Focus on need counts, missing count, left and right.
