id: daily_temperatures
title: Daily Temperatures
category: dsa
level: important_problem
languages:
  - python
  - cpp
  - java
tags:
  - important_problem
  - interview
  - linked_list_and_stack
goal: Teach Daily Temperatures as an interview problem with recognition cue, approach, state tracking, edge cases, coordinated code, and language caveats.
assets:
  daily_temperatures_trace:
    type: image
    path: assets/dsa/daily_temperatures/daily-temperatures-trace.png
    alt: Daily Temperatures variable trace and interview dry run.
lesson:
  - explain:
      title: Solve
      body: >
        Solve: Given daily temperatures, return for each day how many days must pass until a warmer
        temperature appears.
  - compare_table:
      title: When Should This Come To Mind?
      columns:
        - When The Problem Looks Like This
        - Reach For
        - First Coding Move
      rows:
        -
          - "   gives nested structure, previous greater or smaller, histogram boundaries, or undo-like behavior."
          - " stack or monotonic stack."
          - "Store indices when distance, width, or stale position matters; state the stack invariant."
        -
          - "The current item resolves earlier items in last-in-first-out order."
          - ""
          - ""
        -
          - "The unresolved candidates must keep a useful order."
          - ""
          - ""
  - explain:
      title: When To Use This
      body: >
        Bring this idea in when the problem is quietly asking for monotonic stack resolving next warmer day. Before
        coding, say what the input shape is, what answer must be returned, and which
        state proves progress.
  - explain:
      title: Approach And State
      body: >
        Track stack of unresolved indices and answer array. The important interview move is to explain when this state
        changes, why that timing is safe, and which cases are intentionally skipped
        or resolved.
  - compare_table:
      title: Variables And Edge Cases
      columns:
        - Variable Or Entity
        - Meaning
        - Interview Caveat
      rows:
        -
          - "state"
          - "stack of unresolved indices and answer array"
          - "Name this before writing code."
        -
          - "update timing"
          - "the line where progress becomes official"
          - "Most bugs happen before or after this line."
        -
          - "edge case"
          - "empty, one item, duplicates, boundary, or unreachable state"
          - "Test it after the main dry run."
  - language_lens:
      language: python
      title: Python Strategy
      body: >
        Keep the solution compact, but make the state variable names explicit enough for a dry run.
      default_instincts:
        - Use dictionaries, sets, deque, heapq, or lists based on the pattern.
        - Prefer tuple state for immutable compound keys.
        - Watch list aliasing and recursion depth.
      avoid:
        - Hiding an O n operation inside a loop.
        - Using mutable objects as dictionary keys.
        - Updating the answer before the state is valid.
  - language_lens:
      language: cpp
      title: C++ Strategy
      body: >
        Choose containers and numeric types deliberately, and avoid copying large state inside helpers.
      default_instincts:
        - Pass large vectors, strings, and adjacency lists by reference.
        - Use long long when counts, products, sums, or distances can grow.
        - Configure comparators and heaps explicitly.
      avoid:
        - Passing vectors by value accidentally.
        - Using unsafe int sentinels with addition.
        - Dereferencing null pointers in linked-list or tree problems.
  - language_lens:
      language: java
      title: Java Strategy
      body: >
        Use explicit arrays, initialized collections, helper methods, and safe comparator or equality choices.
      default_instincts:
        - Use primitive arrays for fixed-size state.
        - Use ArrayDeque for queues and stacks.
        - Initialize every nested collection before adding to it.
      avoid:
        - Comparator subtraction when values can be large.
        - Double equals for string value comparison.
        - Forgetting null checks on node references.
examples:
  - code_compare:
      title: Daily Temperatures Core Implementation
      idea: monotonic stack resolving next warmer day.
      python: |
        ans = [0] * len(temps)
        stack = []
        for i, t in enumerate(temps):
            while stack and temps[stack[-1]] < t:
                j = stack.pop()
                # i is the first warmer day for unresolved index j.
                ans[j] = i - j
            stack.append(i)
      cpp: |
        vector<int> ans(temps.size(), 0), st;
        for (int i = 0; i < temps.size(); i++) {
            while (!st.empty() && temps[st.back()] < temps[i]) {
                int j = st.back();
                st.pop_back();
                // i is the first warmer day for unresolved index j.
                ans[j] = i - j;
            }
            st.push_back(i);
        }
      java: |
        int[] ans = new int[temps.length];
        Deque<Integer> st = new ArrayDeque<>();
        for (int i = 0; i < temps.length; i++) {
            while (!st.isEmpty() && temps[st.peek()] < temps[i]) {
                int j = st.pop();
                // i is the first warmer day for unresolved index j.
                ans[j] = i - j;
            }
            st.push(i);
        }
      note: >
        Read the code by following stack of unresolved indices and answer array. The comments mark non-trivial state
        changes only, so the explanation and implementation stay coordinated.
interview_questions:
  - question: What is the pattern clue for Daily Temperatures?
    answer: monotonic stack resolving next warmer day.
  - question: What variables should you dry-run?
    answer: Dry-run stack of unresolved indices and answer array, plus the answer variable and any boundary pointer.
  - question: What edge cases should you test?
    answer: Test empty or minimal input, duplicate values where relevant, boundary indices, and cases where no valid answer exists.
revision:
  quick_summary:
    - "Daily Temperatures is solved by monotonic stack resolving next warmer day."
    - "Working idea: track stack of unresolved indices and answer array, update it at the safe moment, and dry-run the edge case that can break the invariant."
  cheat_table:
    title: Daily Temperatures Quick Recall
    columns:
      - Remember
      - Why
    rows:
      -
        - "Name the state"
        - "The code becomes readable immediately."
      -
        - "Explain update timing"
        - "This proves correctness."
      -
        - "Test the smallest input"
        - "This catches most boundary mistakes."
  flashcards:
    - question: What should you say if asked why this works?
      answer: State the invariant and explain why each update preserves it.
notes:
  - Keep comments around meaningful state changes only.
  - In interviews, explain the brute force briefly, then point to the repeated work removed by this pattern.
tlm_hooks:
  possible_tasks:
    - trace_daily_temperatures
    - generate_daily_temperatures_drills
  context: Focus on stack of unresolved indices and answer array.
